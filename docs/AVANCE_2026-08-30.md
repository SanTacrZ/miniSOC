# Avance 2026-08-30 — Entrenamiento SOC listo para ti

> Mientras caminabas, el equipo purple-team construyó y verificó el Mini-SOC completo.

## 1. Qué se hizo hoy (commits en https://github.com/SanTacrZ/miniSOC)

### [aebd501] Bootstrap + Repository Pattern
- API segura con patrón Repositorio: `IUser/Record/Audit` → `FileUserRepo` + `InMemRecordRepo` + `FileAuditRepo` + `AuthService/RecordService`
- JWT RS256 + MFA TOTP + RBAC least privilege (NIST AC-2/3/5/6, IA-2(1))
- SIEM engine + 5 Sigma rules, docs NIST completos
- Fix librerías corruptas (cryptography 44.0.2→46.0.5, pydantic 2.11.7→2.13.5)

### [8d22553] Cloud Audit + Labs + Infra
- 7 checks NIST sobre `infra/terraform/main.tf` con misconfigs intencionales (NET-001 0.0.0.0/0:22, STO-001 public bucket, IAM-001 wildcard)
- Labs Rustyapa instrumentado (wrapper tee → `logs/rustyapa.jsonl`) + exploits L1-L3
- infra docker-compose + scenarios brute/exfil
- 9/9 tests pass, 10 alertas verificadas

## 2. Arquitectura verificada (purple-team)

```
[api:8000] --JSONL hash-chain--> [siem engine] --> alerts/ + timeline
   |  repository pattern              | Sigma (5 reglas ATT&CK)
   + auth_svc / record_svc           + correlations
[cloud_audit] --> findings.json --> siem (FAIL 3/7)
[rustyapa:11331] --wrapper--> rustyapa.jsonl --> SOC-002 high
```

- **Tests:** `pytest api/tests` 9 passed (test_auth 6 + test_repo_pattern 3)
- **Incidentes simulados:**
  - `simulate_incident.sh brute-force` → 7× auth_fail 10.33.7.42 → alerta SOC-001 medium ✅
  - `simulate_incident.sh rustyapa-pwn` → payload 5000 → 8× SOC-002 high ✅
  - `simulate_incident.sh exfil` → 5× suspicious_note → SOC-004 medium ✅
  - `python -m cloud_audit.checks.runner` → 3 FAIL critical (NET-001, NET-002, STO-001) ✅

## 3. Cómo entrenar ahora (cuando vuelvas)

```bash
cd ~/Documents/santacruz/SOC
source .venv/bin/activate
bash scripts/bootstrap_iam.sh
bash scripts/check_libs.sh
python -m pytest api/tests -v
bash scripts/simulate_incident.sh all
python -m siem.engine.engine &  # poll
cat logs/alerts.jsonl | jq
cat logs/siem.jsonl | jq 'select(.action=="auth_fail")'
python siem/engine/timeline.py --out labs/evidence/timeline.jsonl
python -m cloud_audit.checks.runner --verbose
# Intenta romper la API: ver docs/break_it.md
python labs/rustyapa/exploit.py --level 3 --size 5000
```

## 4. Próximos pasos (roadmap)

- [ ] PostgresUserRepository + Alembic (reemplazar File)
- [ ] SIEM con OpenSearch + dashboards Grafana
- [ ] SOAR auto-containment (block IP en siem/engine/responder.py)
- [ ] CI GitHub Actions (pytest + pip check + cloud_audit)

## 5. Repo

- https://github.com/SanTacrZ/miniSOC — branches: main, commits documentados, CHANGELOG.md, docs/repository_pattern.md

---
*Cuida esa espalda. El sistema queda listo para que lo rompas y aprendas SOC como en producción.*
